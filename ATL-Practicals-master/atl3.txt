#Steps To Complete Experiment No. 3 :- 

### **1. Install Ruby**  
- Download **Ruby+Devkit 3.0.1** (or later) from:  
[https://rubyinstaller.org/downloads/].
- Run the installer, check **"Add Ruby to PATH"**, and install the **MSYS2 development toolchain** when prompted.  

### **2. Install Rails**  
```cmd
gem install rails -v 6.1.3.2
```

### **3. Install Node.js & Yarn (Required for Rails 6+)**  
- Download Node.js (LTS) from: [https://nodejs.org/](https://nodejs.org/)  
- Install Yarn via:  
```cmd
npm install -g yarn
```

## **Step 4: Set Up a Database**  

### ** Use SQLite (Default, Simplest for Windows)**  
✅ Already included with RubyInstaller. No extra setup needed.  

### **step 5. Navigate into the App & Set Up the Database**  
```cmd
cd todoapp(Make sure that You Have Saved Your File in C Directory like this :- C:/Users/owner's_name/todoapp2)
```

###***Step 6. Run The Following Commands**
```cmd
1. Bundle Install
2. rails db:migrate
3. rails server.(This Will Run Your Web App In Port No. 3000).
```

